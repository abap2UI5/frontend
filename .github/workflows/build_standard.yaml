name: build_standard

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build_standard
  cancel-in-progress: false

jobs:
  build_standard:
    uses: ./.github/workflows/build_branch.yaml
    with:
      branch: standard
