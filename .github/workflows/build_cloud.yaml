name: build_cloud

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build_cloud
  cancel-in-progress: false

jobs:
  build_cloud:
    uses: ./.github/workflows/build_branch.yaml
    with:
      branch: cloud
