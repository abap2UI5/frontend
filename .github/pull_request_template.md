<!--
  Before you write anything: does this change belong here?

  app/webapp/**  is MIRRORED from abap2UI5 and force-overwritten on every push
                 to abap2UI5 main. Change it in abap2UI5/app/webapp instead and
                 run 'npm run app2abap' there.
  Any branch     other than main is GENERATED from main and rebuilt from
                 scratch. Target main.

  The guard_mirrored workflow fails the pull request if either applies.
  Only abap/, .github/ and the docs are maintained in this repository.
-->

## What changes

## Why
