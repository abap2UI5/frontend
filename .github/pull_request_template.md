<!--
  STOP - this repository does not take manual pull requests.

  abap2UI5/frontend is a delivery repository. Its content is written by
  automation, not by hand:

    app/webapp/**  is MIRRORED from abap2UI5 and force-overwritten on every
                   push to abap2UI5 main. Change it in abap2UI5/app/webapp
                   instead and run 'npm run app2abap' there.
    any branch     other than main is GENERATED from main and rebuilt from
                   scratch. Target main.

  A change made in the wrong place is not rejected - it is silently discarded
  on the next automated run, days after it was merged. So the guard_mirrored
  check fails EVERY pull request here by default.

  The one exception is this repository's own substance - abap/, .github/ and
  the docs - because nothing generates those. A maintainer unlocks such a pull
  request with the label 'maintenance', which re-runs the check. Ask for it
  below; do not work around the gate.

  Background: CONTRIBUTING.md
  Bugs and feature requests: https://github.com/abap2UI5/abap2UI5/issues
-->

## What changes

## Why

## Why this belongs in *this* repository

<!--
  Required. Name the owned area you are touching - abap/, .github/ or docs -
  and confirm the change is not mirrored from abap2UI5 and not generated from
  main. Then ask a maintainer for the 'maintenance' label.

  If you cannot fill this in, the change almost certainly belongs in
  https://github.com/abap2UI5/abap2UI5 - please close this and open it there.
-->
